# DO NOT USE THIS FILE IN PRODUCTION
# This is a template for local development only
# In production, use external-secrets-operator.yaml with Vault or AWS Secrets Manager
#
# For local development:
# 1. Copy this file to secrets.yaml
# 2. Replace placeholder values with actual development secrets
# 3. Add secrets.yaml to .gitignore
#
# Example:
# cp secrets-template.yaml secrets.yaml
# # Edit secrets.yaml with your values
# kubectl apply -f secrets.yaml

apiVersion: v1
kind: Secret
metadata:
  name: ml-service-secrets
  namespace: waqiti-system
  labels:
    app: ml-service
    component: secrets
    environment: development
type: Opaque
data:
  # Base64 encoded secrets - replace with actual values
  db-host: REPLACE_WITH_BASE64_ENCODED_VALUE
  db-username: REPLACE_WITH_BASE64_ENCODED_VALUE
  db-password: REPLACE_WITH_BASE64_ENCODED_VALUE
  redis-password: REPLACE_WITH_BASE64_ENCODED_VALUE
  jwt-secret: REPLACE_WITH_BASE64_ENCODED_VALUE
  influxdb-token: REPLACE_WITH_BASE64_ENCODED_VALUE
  neo4j-username: REPLACE_WITH_BASE64_ENCODED_VALUE
  neo4j-password: REPLACE_WITH_BASE64_ENCODED_VALUE