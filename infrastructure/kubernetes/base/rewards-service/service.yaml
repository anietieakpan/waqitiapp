apiVersion: v1
kind: Service
metadata:
  name: rewards-service
  namespace: waqiti
  labels:
    app: rewards-service
    component: backend
spec:
  type: ClusterIP
  selector:
    app: rewards-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP