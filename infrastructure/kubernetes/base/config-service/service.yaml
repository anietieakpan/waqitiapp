apiVersion: v1
kind: Service
metadata:
  name: config-service
  namespace: waqiti
  labels:
    app: config-service
    component: infrastructure
spec:
  type: ClusterIP
  selector:
    app: config-service
  ports:
  - name: http
    port: 8888
    targetPort: 8888
    protocol: TCP