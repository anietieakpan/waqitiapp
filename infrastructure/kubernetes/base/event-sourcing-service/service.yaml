apiVersion: v1
kind: Service
metadata:
  name: event-sourcing-service
  namespace: waqiti-system
  labels:
    app: event-sourcing-service
    component: backend
    tier: application
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8096"
    prometheus.io/path: "/actuator/prometheus"
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
  - name: http
    port: 8096
    targetPort: 8096
    protocol: TCP
  - name: management
    port: 8097
    targetPort: 8097
    protocol: TCP
  selector:
    app: event-sourcing-service

---
apiVersion: v1
kind: Service
metadata:
  name: event-sourcing-service-headless
  namespace: waqiti-system
  labels:
    app: event-sourcing-service
    component: backend
    tier: application
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: 8096
    targetPort: 8096
    protocol: TCP
  selector:
    app: event-sourcing-service