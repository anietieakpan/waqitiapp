apiVersion: v1
kind: Service
metadata:
  name: group-payment-service
  namespace: waqiti
  labels:
    app: group-payment-service
    component: backend
spec:
  type: ClusterIP
  selector:
    app: group-payment-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP