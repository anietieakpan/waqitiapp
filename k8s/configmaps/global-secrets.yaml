# =============================================================================
# PLACEHOLDER SECRETS - FOR DEMONSTRATION/TEMPLATE PURPOSES ONLY
# =============================================================================
#
# WARNING: These are NOT real secrets. All values are placeholders that decode
# to obvious fake values like "not_real_token", "kafka_pass", etc.
#
# FOR PRODUCTION:
# 1. Generate real secrets using a secure method
# 2. Store them in HashiCorp Vault (recommended) or use sealed-secrets
# 3. Enable KMS encryption for etcd at rest
# 4. Never commit real secrets to version control
#
# Example of generating a secure secret:
#   openssl rand -base64 32 | base64
#
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: vault-token
  namespace: example-prod
  labels:
    environment: production
  annotations:
    description: "PLACEHOLDER - Replace with real Vault token"
type: Opaque
data:
  # CONFIGURE_IN_VAULT: Replace with actual base64-encoded secret
  token: CHANGE_ME_BASE64_ENCODED

---
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secret
  namespace: example-prod
  labels:
    environment: production
  annotations:
    description: "PLACEHOLDER - Replace with real Keycloak client secret"
type: Opaque
data:
  # CONFIGURE_IN_VAULT: Replace with actual base64-encoded secret
  client-secret: CHANGE_ME_BASE64_ENCODED

---
apiVersion: v1
kind: Secret
metadata:
  name: kafka-credentials
  namespace: example-prod
  labels:
    environment: production
  annotations:
    description: "PLACEHOLDER - Replace with real Kafka credentials"
type: Opaque
data:
  # CONFIGURE_IN_VAULT: Replace with actual base64-encoded secret
  username: CHANGE_ME_BASE64_ENCODED
  # CONFIGURE_IN_VAULT: Replace with actual base64-encoded secret
  password: CHANGE_ME_BASE64_ENCODED

---
apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
  namespace: example-prod
  labels:
    environment: production
  annotations:
    description: "PLACEHOLDER - Replace with real Redis password"
type: Opaque
data:
  # CONFIGURE_IN_VAULT: Replace with actual base64-encoded secret
  password: CHANGE_ME_BASE64_ENCODED
